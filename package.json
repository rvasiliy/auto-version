{
  "name": "auto-version",
  "version": "2.1.38",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "HUSKY_SKIP_HOOKS=1 npm version --no-commit-hooks --no-git-tag-version patch && git add --all"
    }
  }
}
